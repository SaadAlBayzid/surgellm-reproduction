{
  "model_name": "roberta-base",
  "seed": 0,
  "num_labels": 2,
  "learning_rate": 0.00002,
  "weight_decay": 0.01,
  "adam_betas": [0.9, 0.999],
  "adam_epsilon": 1e-8,
  "max_epochs": 3,
  "max_length": 128,
  "warmup_fraction": 0.06,
  "gradient_clip_norm": 1.0,
  "micro_batch_size": 4,
  "gradient_accumulation_steps": 8,
  "eval_batch_size": 4,
  "early_stopping_patience": 2,
  "classifier_dropout": 0.1,
  "device": "cpu",
  "precision": "fp32",
  "cpu_threads": 4,
  "split_policy": "cap_then_holdout",
  "training_cap": 7666,
  "smoke_train_examples": 128,
  "smoke_validation_examples": 24,
  "smoke_epochs": 1,
  "temporary_choices": {
    "split": "AMBIGUOUS: choose cap_then_holdout; cap 7666 then ceil(10%) internal validation; official dev is reserved and not evaluated.",
    "batch": "AMBIGUOUS: choose section 5 effective global batch 32, not Table 13 implied 64. Single CPU: 4 microbatch x 8 accumulation.",
    "head": "AMBIGUOUS: plain Hugging Face RobertaForSequenceClassification standard head (768->768->2, tanh), not the paper's multi-task MLP. Classifier dropout 0.1 at both standard head dropout sites.",
    "tokenization": "AMBIGUOUS: fast tokenizer; preserve raw case/text; right truncation and right padding to 128 including special tokens; add_prefix_space=False; no added vocabulary.",
    "optimizer": "AMBIGUOUS: AdamW decay on all trainable parameters; warmup steps ceil(0.06 * total optimizer steps); linear decay to zero; short final accumulation window uses its actual example count.",
    "runtime": "CPU FP32 smoke due to installed CPU-only PyTorch; paper used 2xT4 FP16 Accelerate. Custom single-device loop; no Accelerate or DDP.",
    "software": "Transformers 4.35.2 selects patch within paper 4.35; existing PyTorch 2.6.0+cpu instead of paper 2.1. Runtime versions saved per run.",
    "metrics": "Argmax logits; binary macro-F1 averages fixed classes [0,1], zero division=0; loss is example-weighted cross entropy; validation covers entire selected subset.",
    "sampling": "Existing loader's Python RNG/largest-remainder stratification and rounding; smoke subsamples 128 training and 24 internal-validation rows after splitting.",
    "revisions": "Resolve current Hugging Face model/dataset revisions once during asset download, then save immutable commit hashes and file hashes; original author revisions unknown.",
    "determinism": "Seed Python, NumPy and Torch; deterministic algorithms enabled; CPU thread count fixed. Bitwise cross-platform equivalence not claimed."
  }
}
